# ─── Production only ────────────────────────────────────
# Leave empty for local development (Vite proxies /api automatically).
#
# When you deploy the proxy (server/) separately, e.g. to Render,
# Railway, or Fly.io, set this to its full URL:
#
# VITE_PROXY_URL=https://veritasai-proxy.onrender.com
VITE_PROXY_URL=
